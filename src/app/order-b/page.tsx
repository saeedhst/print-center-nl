import React from 'react';
import CadDesignIntake from '@/components/quote/CadDesignIntake';

export const metadata = {
  title: 'Track 02: CAD Design & Idea Intake | PrintLab.nl',
  description: 'Upload sketches, photos of broken parts, or links. Our Amsterdam CAD engineers design the 3D model with a guaranteed 24h quote.',
};

export default function OrderBPage() {
  return <CadDesignIntake />;
}

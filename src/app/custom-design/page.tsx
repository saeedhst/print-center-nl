import React from 'react';
import CadDesignIntake from '@/components/quote/CadDesignIntake';

export const metadata = {
  title: 'Custom Parametric CAD Design Service | PrintLab.nl Amsterdam',
  description: 'Upload sketches, broken parts, or ideas. Industrial CAD engineers in Haarlem, Amsterdam & Utrecht will create your printable 3D model with a guaranteed 24h quote.',
};

export default function CustomDesignPage() {
  return <CadDesignIntake />;
}
